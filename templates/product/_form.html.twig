{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}

{% set hasError = form.name.vars.errors|length > 0 %}
<div class="form-group mb-3">
    {{ form_label(form.name) }}
    {{ form_widget(form.name, {
        'attr': {'class': 'form-control ' ~ (hasError ? 'is-invalid' : '')}
    }) }}
    {% if hasError %}
        <div class="invalid-feedback d-block">
            {{ form_errors(form.name) }}
        </div>
    {% endif %}
</div>

{% set hasError = form.sku.vars.errors|length > 0 %}
<div class="form-group mb-3">
    {{ form_label(form.sku) }}
    {{ form_widget(form.sku, {
        'attr': {'class': 'form-control ' ~ (hasError ? 'is-invalid' : '')}
    }) }}
    {% if hasError %}
        <div class="invalid-feedback d-block">
            {{ form_errors(form.sku) }}
        </div>
    {% endif %}
</div>

{% set hasError = form.shortDescription.vars.errors|length > 0 %}
<div class="form-group mb-3">
    {{ form_label(form.shortDescription) }}
    {{ form_widget(form.shortDescription, {
        'attr': {'class': 'form-control ' ~ (hasError ? 'is-invalid' : '')}
    }) }}
    {% if hasError %}
        <div class="invalid-feedback d-block">
            {{ form_errors(form.shortDescription) }}
        </div>
    {% endif %}
</div>

{% set hasError = form.description.vars.errors|length > 0 %}
<div class="form-group mb-3">
    {{ form_label(form.description) }}
    {{ form_widget(form.description, {
        'attr': {'class': 'form-control ' ~ (hasError ? 'is-invalid' : '')}
    }) }}
    {% if hasError %}
        <div class="invalid-feedback d-block">
            {{ form_errors(form.description) }}
        </div>
    {% endif %}
</div>

{% set hasError = form.price.vars.errors|length > 0 %}
<div class="form-group mb-3">
    {{ form_label(form.price) }}
    {{ form_widget(form.price, {
        'attr': {'class': 'form-control ' ~ (hasError ? 'is-invalid' : '')}
    }) }}
    {% if hasError %}
        <div class="invalid-feedback d-block">
            {{ form_errors(form.price) }}
        </div>
    {% endif %}
</div>

{% set hasError = form.discountPrice.vars.errors|length > 0 %}
<div class="form-group mb-3">
    {{ form_label(form.discountPrice) }}
    {{ form_widget(form.discountPrice, {
        'attr': {'class': 'form-control ' ~ (hasError ? 'is-invalid' : '')}
    }) }}
    {% if hasError %}
        <div class="invalid-feedback d-block">
            {{ form_errors(form.discountPrice) }}
        </div>
    {% endif %}
</div>

{% set hasError = form.stock.vars.errors|length > 0 %}
<div class="form-group mb-3">
    {{ form_label(form.stock) }}
    {{ form_widget(form.stock, {
        'attr': {'class': 'form-control ' ~ (hasError ? 'is-invalid' : '')}
    }) }}
    {% if hasError %}
        <div class="invalid-feedback d-block">
            {{ form_errors(form.stock) }}
        </div>
    {% endif %}
</div>

{% set hasError = form.category.vars.errors|length > 0 %}
<div class="form-group mb-3">
    {{ form_label(form.category) }}
    {{ form_widget(form.category, {
        'attr': {'class': 'form-control ' ~ (hasError ? 'is-invalid' : '')}
    }) }}
    {% if hasError %}
        <div class="invalid-feedback d-block">
            {{ form_errors(form.category) }}
        </div>
    {% endif %}
</div>

{% set hasError = form.isActive.vars.errors|length > 0 %}
<div class="form-group form-check mb-3">
    {{ form_widget(form.isActive, {
        'attr': {'class': 'form-check-input ' ~ (hasError ? 'is-invalid' : '')}
    }) }}
    {{ form_label(form.isActive, null, {'label_attr': {'class': 'form-check-label'}}) }}
    {% if hasError %}
        <div class="invalid-feedback d-block">
            {{ form_errors(form.isActive) }}
        </div>
    {% endif %}
</div>

{% set hasError = form.imageFile.vars.errors|length > 0 %}
<div class="form-group mb-3">
    {{ form_label(form.imageFile) }}
    {{ form_widget(form.imageFile, {
        'attr': {'class': hasError ? 'is-invalid' : ''}
    }) }}
    {% if hasError %}
        <div class="invalid-feedback d-block">
            {{ form_errors(form.imageFile) }}
        </div>
    {% endif %}
</div>

<button class="btn btn-primary mt-2">
    {{ button_label|default('Save') }}
</button>

{{ form_end(form) }}